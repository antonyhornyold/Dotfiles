- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    # ~/.vim: vim - kept as an example
    ~/.zshrc: zshrc

- create:
    - ~/Repos

# - shell:
  # - [git submodule update --init --recursive, Installing submodules]
